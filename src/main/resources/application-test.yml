spring:
  datasource:
    driver-class-name: org.h2.driver
    url: jdbc:h2:~/test

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
